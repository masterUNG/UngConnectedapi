import 'package:flutter/material.dart';

class BodyListData extends StatelessWidget {
  const BodyListData({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('This is List Data');
  }
}